# Application Name
spring.application.name=subscription-tracker

# Server Port
server.port=8084

# PostgreSQL Database Configuration (Local Development)
# For production, set environment variables: DB_URL, DB_USERNAME, DB_PASSWORD
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/subscription_tracker_db}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Adi@1608}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration
# For production, set environment variable: JWT_SECRET (use a strong random key)
jwt.secret=${JWT_SECRET:0Ung7+X5IbZiZJj/l5ij8YS/sz4nRufrkUfQd5GfrWc=}
jwt.expiration=${JWT_EXPIRATION:86400000}

# ============================================
# EMAIL CONFIGURATION (Gmail SMTP)
# For production, set environment variables: MAIL_USERNAME, MAIL_PASSWORD
# ============================================
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:hactivators007@gmail.com}
spring.mail.password=${MAIL_PASSWORD:wjiqxctvykkwnrgq}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Email sender details
app.email.from=${MAIL_FROM:hactivators007@gmail.com}
app.email.from-name=${MAIL_FROM_NAME:Subscription Tracker}
# CORS Configuration
cors.allowed.origins=http://localhost:3000,http://localhost:5173,http://localhost:5174